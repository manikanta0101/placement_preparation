"""8. Count the number of vowels in a string
Required Input:
education
Expected Output:
5  """

code:
def count_vowels():
    s = "education"
    vowels = 'aeiouAEIOU'
    count = 0
    for r in s:
        if r in vowels:
            count += 1
    return count

print(count_vowels())

output:
5
